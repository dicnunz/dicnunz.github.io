# Project demos

Built output and saved reports from the linked project repositories. `sources.json` records the source commits. Falling Sand and Import Graph run in the browser. Property Check and the asyncio experiment show real saved runs; Codex Sessions uses an explicitly labeled synthetic hub. The certificate inspector shows one verified coloring record.

Display names in these snapshots match the portfolio; compatibility paths and saved report data retain their original names. To reproduce or change a project, use its source repository.

# Project demos

Built output and saved reports from the linked project repositories. `sources.json` records the source commits. PixelMelt and Boundary Atlas run in the browser. Counterexample Studio and the asyncio experiment show real saved runs; Mission Control uses an explicitly labeled synthetic hub. The certificate inspector shows one verified coloring record.

These snapshots are updated alongside reviewed source changes. To reproduce or change a project, use its source repository.
